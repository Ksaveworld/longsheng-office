# 2026-09-16 源码同步

当前办公协同源码与 [在线演示](https://longsheng-office-0910.vercel.app/office#home) 的部署 `dpl_FpQmF7iEjWGWVdVJxzhP2ezjWHwc` 对应。

本次更新围绕既有供应延期事项：配套合成 CSV 导入、对象与订单关联、程序条件筛选和日期计算、命中及排除说明、零结果解释、原始行来源和受限的引用重新核对。既有质量核验、负责人批准、另行发送、回执和关闭流程保留。

线上实际验证：A 由 D6 改为 D9 后，两张订单分别晚 4 天、1 天；真实模型“至少晚 2 天”命中 1 条，“至少晚 5 天”返回 0 条；来源面板可查看原始 CSV 行。刷新后保留导入状态，访客空间相互隔离。公开入口保留对重置和故障注入的禁止。

交付补齐：将订单追溯测试所需的外部合成样例移入 `tests/fixtures`；本地启动器统一使用 Node 24 执行 npm，使用项目内缓存，并采用正常关闭方式。应用业务代码、构建配置与锁文件保持对应上线版本。

独立解压目录执行 144 项 office 测试通过。依赖按锁文件重新安装，TypeScript / Vite 构建通过；本地规则模式启动检查覆盖前端页面、后端快照与模式状态。真实模型需自行配置服务端环境变量，源码不包含在线模型凭据。

此次采用新提交更新现有仓库，没有重写历史。旧 `docs` 与 `deploy` 材料保留作历史参考，当前使用方式以根 README 为准。数据仍为合成样例，不代表客户生产数据或目标人力模块已接通。
